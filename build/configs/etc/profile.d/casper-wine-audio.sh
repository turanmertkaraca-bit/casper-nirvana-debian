# CasperOS — Wine audio buffers for Bay Trail Atom
# PULSE_LATENCY_MSEC=60        : 60 ms latency (default is lower -> crackles)
# STAGING_AUDIO_DURATION=20000 : extra buffer (us) for wine-staging builds
export PULSE_LATENCY_MSEC=60
export STAGING_AUDIO_DURATION=20000
